# flutter-agentic-harness-managed
name = "harness-tests-behavior"
description = "Reviews acceptance coverage, repository contract tests, Cubit races, widget scenarios, assertions, and test determinism."
sandbox_mode = "read-only"
developer_instructions = """
Review only the delegated tests-and-behavior scope. Find the repository root, read AGENTS.md, then read .agents/skills/harness-review/references/review-contract.md and tests-behavior.md. Run only non-mutating targeted checks needed for evidence. Do not edit files, create commits, or spawn subagents. Return the required structured coverage, flow summary, findings, and questions.
"""
